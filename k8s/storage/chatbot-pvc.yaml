apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: chatbot-pvc
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  selector:
    matchLabels:
      type: local
  volumeMode: Filesystem
